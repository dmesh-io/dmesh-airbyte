{
  "connectionSpecification" : {
    "$schema" : "http://json-schema.org/draft-07/schema#",
    "additionalProperties" : true,
    "properties" : {
      "auth_token" : {
        "airbyte_secret" : true,
        "title" : "auth_token",
        "type" : "string"
      },
      "format" : {
        "default" : "json",
        "enum" : [
          "json",
          "xml",
          "csv",
          "tsv"
        ],
        "title" : "format",
        "type" : "string",
        "description": "defines the format of the output"
      },
      "id_site" : {
        "default" : 3,
        "title" : "id_site",
        "type" : "integer"
      },
      "organization_name" : {
        "title" : "Organization Name",
        "type" : "string"
      },
      "period": {
        "title": "period",
        "description": " the period you request the statistics for. Can be any of: day, week, month, year or range. All reports are returned for the dates based on the website's time zone.",
        "enum": [
          "day",
          "week",
          "year",
          "range"
        ],
        "default": "json",
        "type": "string"
      },
      "filter_limit": {
        "title": "filter_limit",
        "description": "defines the number of rows to be returned. By default, only the top 100 rows are returned. Set to -1 to return all rows.",
        "type": "integer"
      },
      "segment": {
        "title": "segment",
        "description": "defines the Custom Segment you wish to filter your reports to.",
        "type": "string"
      },
      "date": {
        "title": "date",
        "description": "standard format = YYYY-MM-DD. magic keywords = today, yesterday, lastWeek, lastMonth or lastYear. These are relative the website timezone. For example, for a website with UTC+12 timezone, date=today for an API request at 5PM UTC on 2010-01-01 will return the reports for 2010-01-02. ",
        "type": "string",
        "default": "yesterday"
      }
    },
    "required" : [
      "format",
      "id_site",
      "auth_token",
      "organization_name"
    ],
    "type" : "object"
  },
  "documentationUrl" : "https://example.org"
}